* Updated the [[Welcome]] note
* activated some plugin for obsidian: Mermaid tool and editing toolbar. Not sure if I'll continue with the toolbar plugin. Graphs are important though
* Strange issue with  noticed when executing `npx quartz sync` where the vault becomes empty. Seems that adding `--no-pull` doesn't cause issue…
	* Interestingly enough, you need to use the `--no-pull` flag once. Then afterwards the `npx quartz sync` is working find
